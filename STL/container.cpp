#include <bits/stdc++.h>

using namespace std;

int main()
{
    // array container
    //  array<int, 3> arr; //->{?,?,?}
    // array<int, 5> arr = {1, 5, 6}; //->{1, 5, 6, 0, 0}
    // cout << arr[4];

    /*
    array<int, 5> arr;
       arr.fill(0);  // ->{0,0,0,0,0}
       arr.fill(10); // ->{10,10,10,10,10}
       for (int i = 0; i < 5; i++)
           cout << arr.at(i) << endl;
    */

    return 0;
}